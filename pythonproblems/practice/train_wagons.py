from itertools import cycle
import random

def get_size(train):

    circle_train = cycle(train)
    position = random.randint(0, len(train))








lst = ['a', 'b', 'c']

pool = cycle(lst)
print(pool[5])

# for item in pool:
#     print(item)